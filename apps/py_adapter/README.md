# gokart-station Python adapter

This package is the local Python adapter scaffold for `gokart-station`.

It is intentionally kept separate from any target gokart repository so the station repo and the
target repo remain independent.
